BROKER="192.168.23.217"
PORT="1883"
USER="clage"
PASS="control"

# TOPIC: if empty, auto-derived as "chs-" + last 4 chars of MAC (e.g. chs-36FF)
TOPIC=""
FRIENDLY_NAME="CLAGE Home Server"
# DISCOVERY_ID: if empty, auto-derived as MAC address without colons, uppercase (e.g. CCFFE0FF36FF)
DISCOVERY_ID=""

INTERVAL="30"
TASMOTA_DISCOVERY="1"
